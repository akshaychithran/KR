#include <stdio.h>
int main()
{
	int a, i,nwhite,nother;
	int ndigit[10];
	nwhite = nother = 0;
	for (i = 0; i < 10;++i)
	ndigit[i] = 0;
	while ((a = getchar()) != EOF)
	{
		if (c >= '0' && c<='9')
		 	++ndigit[
